<Window x:Class="Warp9.Forms.RepeatedMeasurementsConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warp9.Forms"
        xmlns:controls="clr-namespace:Warp9.Themes"
        Style="{StaticResource CustomWindowStyle}"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="Repeated Measurements" Height="450" Width="450">
    <Window.Resources>
        <DropShadowBitmapEffect x:Key="NormalShadow" ShadowDepth="2" Softness="0.2" Opacity="0.64"/>
        <local:RepeatedMeasurementsOperationRadioConverter x:Key="convertOperation"/>
    </Window.Resources>
    
    <DockPanel Margin="4">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Height="28" Margin="4" HorizontalAlignment="Right">
            <Button x:Name="btnOK" Content="OK" Width="100" Style="{DynamicResource ControlButton}"/>
            <Label Width="4" />
            <Button x:Name="btnCancel" Content="Cancel" Width="100" Style="{DynamicResource ControlButton}"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Width="400">
            <TextBlock Style="{DynamicResource GroupBoxHeaderBlock}" Margin="2">Series assignment</TextBlock>
            <Label Height="4" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="28"/>
                    <RowDefinition Height="28"/>                  
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition />          
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">Series identifier</TextBlock>
                <ComboBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" 
                          ItemsSource="{Binding SearchableColumns, UpdateSourceTrigger=PropertyChanged}" 
                          SelectedIndex="{Binding SeriesColumnIndex, Mode=TwoWay}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">Independent variable</TextBlock>
                <ComboBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" 
                          ItemsSource="{Binding SearchableColumns, UpdateSourceTrigger=PropertyChanged}"
                          SelectedIndex="{Binding OrderColumnIndex, Mode=TwoWay}"
                          SelectionChanged="OrderColumn_SelectionChanged"/>
            </Grid>

            <Label Height="8" />

            <TextBlock Style="{DynamicResource GroupBoxHeaderBlock}" Margin="2">Series operation</TextBlock>
            <Label Height="4" />
            <RadioButton GroupName="type" IsChecked="{Binding Path=Operation, Mode=TwoWay, Converter={StaticResource convertOperation}, ConverterParameter=TwoPointDifference}">Two points</RadioButton>
            <TextBlock Style="{DynamicResource TextBlockDimmed}" TextWrapping="Wrap" Margin="12,4,0,0">In each series, computes the difference between two data points if available.</TextBlock>
            <Label Height="4" />

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="28"/>
                    <RowDefinition Height="28"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">First</TextBlock>
                <ComboBox Grid.Row="0" Grid.Column="2" VerticalAlignment="Center"
                          ItemsSource="{Binding OrderValueLevels, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="1" Grid.Column="1" VerticalAlignment="Center">Second</TextBlock>
                <ComboBox Grid.Row="1" Grid.Column="2" VerticalAlignment="Center"
                          ItemsSource="{Binding OrderValueLevels, UpdateSourceTrigger=PropertyChanged}" />

            </Grid>

            <Label Height="8" />

            <RadioButton GroupName="type" IsChecked="{Binding Path=Operation, Mode=TwoWay, Converter={StaticResource convertOperation}, ConverterParameter=LinearRegression}">Linear regression</RadioButton>
            <TextBlock Style="{DynamicResource TextBlockDimmed}" TextWrapping="Wrap" Margin="12,4,0,0">Computes the slope of a linear model fitted to all points in the series.</TextBlock>

        </StackPanel>
        
    </DockPanel>
</Window>
